services:
  backend:
    build:
      context: . # Контекст — корінь, щоб бачити всю структуру
      dockerfile: backend/Dockerfile
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=dev # Ваш профіль зі стабами
